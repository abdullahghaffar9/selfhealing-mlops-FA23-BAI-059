groups:
  - name: sentiment-alert-group
    rules:
      - alert: ModelConfidenceDrift
        expr: prediction_confidence_score < 0.68
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Model confidence drift detected"
          description: "Prediction confidence score has dropped below 0.68."
